import { Building2 } from "lucide-react";import { redirect } from "next/navigation";import { apiFetch,requireSession } from "@/lib/server-api";
interface Company{publicId:string;displayName:string;legalName:string;slug:string;status:string;plan:{name:string}|null;_count:{users:number;branches:number;pixCharges:number}}
export default async function CompaniesPage(){const principal=await requireSession();if(!principal.permissions.includes("platform.companies.read"))redirect("/nova-venda");const companies=(await apiFetch<{data:Company[]}>("/platform/companies?pageSize=100")).data;return <div className="page-container management-page"><div className="management-heading"><div><p className="eyebrow">Clientes</p><h1 className="display-title">Empresas</h1><p>Planos, limites e estado das organizações clientes.</p></div></div><section className="card data-card"><div className="table-scroll"><table><thead><tr><th>Empresa</th><th>Plano</th><th>Usuários</th><th>Filiais</th><th>Cobranças</th><th>Estado</th></tr></thead><tbody>{companies.map((company)=><tr key={company.publicId}><td><strong>{company.displayName}</strong><small>{company.slug}</small></td><td>{company.plan?.name??"Sem plano"}</td><td>{company._count.users}</td><td>{company._count.branches}</td><td>{company._count.pixCharges}</td><td><span className={company.status==="ACTIVE"?"positive":"negative"}>{company.status==="ACTIVE"?"Ativa":"Suspensa"}</span></td></tr>)}</tbody></table></div>{!companies.length&&<div className="empty-state"><Building2/><p>Nenhuma empresa cliente cadastrada.</p></div>}</section></div>}
