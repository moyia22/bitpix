"use client";
import { LoaderCircle, Plus, Power, RotateCcw } from "lucide-react";import { useRouter } from "next/navigation";import { useState,type FormEvent } from "react";
const apiUrl=process.env.NEXT_PUBLIC_API_URL??"http://localhost:3333";
type User={publicId:string;name:string;email:string;status:string;branch:{publicId:string;name:string}|null;roles:Array<{role:{key:string;name:string}}>;activeSessionCount:number};type Role={key:string;name:string};type Branch={publicId:string;name:string};
export function UserManager({users,roles,branches,canCreate,canUpdate,canRevoke}:{users:User[];roles:Role[];branches:Branch[];canCreate:boolean;canUpdate:boolean;canRevoke:boolean}){const router=useRouter();const[open,setOpen]=useState(false);const[busy,setBusy]=useState(false);const[notice,setNotice]=useState("");const call=async(path:string,options:RequestInit)=>{const response=await fetch(`${apiUrl}/api/v1${path}`,{...options,credentials:"include",headers:{"content-type":"application/json",...options.headers}});const result=await response.json() as {error?:{message?:string}};if(!response.ok)throw new Error(result.error?.message??"Operação não concluída.");};const create=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setBusy(true);const data=new FormData(event.currentTarget);try{await call("/users",{method:"POST",body:JSON.stringify({name:String(data.get("name")),email:String(data.get("email")),password:String(data.get("password")),branchPublicId:String(data.get("branch"))||null,roleKeys:[String(data.get("role"))]})});setOpen(false);setNotice("Usuário criado. A troca da senha inicial será obrigatória.");router.refresh();}catch(error){setNotice(error instanceof Error?error.message:"Falha.");}finally{setBusy(false);}};const toggle=async(user:User)=>{setBusy(true);try{await call(`/users/${user.publicId}`,{method:"PATCH",body:JSON.stringify({status:user.status==="ACTIVE"?"INACTIVE":"ACTIVE"})});router.refresh();}catch(error){setNotice(error instanceof Error?error.message:"Falha.");}finally{setBusy(false);}};const revoke=async(user:User)=>{setBusy(true);try{await call(`/users/${user.publicId}/revoke-sessions`,{method:"POST"});setNotice(`Sessões de ${user.name} revogadas.`);router.refresh();}catch(error){setNotice(error instanceof Error?error.message:"Falha.");}finally{setBusy(false);}};return <>{canCreate&&<div className="admin-actions"><button className="primary-button" onClick={()=>setOpen(!open)} type="button"><Plus size={17}/>Novo usuário</button></div>}{open&&<form className="card inline-create user-create" onSubmit={create}><label><span>Nome</span><input name="name" required/></label><label><span>E-mail</span><input type="email" name="email" required/></label><label><span>Senha inicial</span><input type="password" name="password" minLength={12} required/></label><label><span>Filial</span><select name="branch"><option value="">Todas</option>{branches.map((branch)=><option value={branch.publicId} key={branch.publicId}>{branch.name}</option>)}</select></label><label><span>Função</span><select name="role" required>{roles.map((role)=><option value={role.key} key={role.key}>{role.name}</option>)}</select></label><button className="primary-button" disabled={busy}>{busy?<LoaderCircle className="spin"/>:<Plus/>}Criar</button></form>}{notice&&<p className="inline-notice">{notice}</p>}<section className="card data-card"><div className="table-scroll"><table><thead><tr><th>Usuário</th><th>Filial</th><th>Função</th><th>Sessões</th><th>Estado</th><th>Ações</th></tr></thead><tbody>{users.map((user)=><tr key={user.publicId}><td><strong>{user.name}</strong><small>{user.email}</small></td><td>{user.branch?.name??"Todas"}</td><td>{user.roles.map(({role})=>role.name).join(", ")}</td><td>{user.activeSessionCount}</td><td><span className={user.status==="ACTIVE"?"positive":"negative"}>{user.status==="ACTIVE"?"Ativo":user.status==="BLOCKED"?"Bloqueado":"Inativo"}</span></td><td><div className="row-actions">{canRevoke&&<button type="button" title="Revogar sessões" disabled={busy} onClick={()=>void revoke(user)}><RotateCcw/></button>}{canUpdate&&<button type="button" title={user.status==="ACTIVE"?"Desativar":"Ativar"} disabled={busy} onClick={()=>void toggle(user)}><Power/></button>}</div></td></tr>)}</tbody></table></div></section></>}
