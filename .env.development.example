# =====================================================================
# BitPix — DEVELOPMENT (local)
# Copie para .env. Postgres/Redis expostos só em 127.0.0.1.
# Fallbacks de desenvolvimento permitidos. Provider em mock.
# =====================================================================
NODE_ENV=development
APP_ENV=development
APP_URL=http://localhost:3000
API_URL=http://localhost:3333
NEXT_PUBLIC_API_URL=http://localhost:3333
API_PORT=3333
API_HOST_PORT=3333
WEB_HOST_PORT=3000
APP_VERSION=0.1.0
APP_COMMIT_SHA=development
APP_BUILD_DATE=unknown

POSTGRES_DB=bitpix
POSTGRES_USER=bitpix
POSTGRES_PASSWORD=change-me-local-only
POSTGRES_PORT=5433
DATABASE_URL=postgresql://bitpix:change-me-local-only@localhost:5433/bitpix?schema=public
# DIRECT_URL: usado só nas migrations (Prisma). Local = igual ao DATABASE_URL.
# Com Supabase, DATABASE_URL = pooler e DIRECT_URL = conexão direta (ver SUPABASE_SETUP.md).
DIRECT_URL=postgresql://bitpix:change-me-local-only@localhost:5433/bitpix?schema=public
DATABASE_POOL_MAX=10

REDIS_PORT=6380
REDIS_URL=redis://localhost:6380

SESSION_COOKIE_NAME=bitpix_session
SESSION_TTL_HOURS=8
SHUTDOWN_TIMEOUT_MS=30000

SEED_ADMIN_EMAIL=admin@bitpix.local
SEED_ADMIN_PASSWORD=

# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
PROVIDER_CREDENTIALS_ENCRYPTION_KEY=
PAYMENT_PROVIDER_MODE=mock
PAYMENT_PROVIDER_TIMEOUT_MS=8000
MERCADO_PAGO_API_BASE_URL=https://api.mercadopago.com
MERCADO_PAGO_WEBHOOK_SECRET=
PUBLIC_WEBHOOK_BASE_URL=http://localhost:3333
WEBHOOK_LOCAL_FALLBACK=true
WEBHOOK_SIGNATURE_TOLERANCE_SECONDS=300
WEBHOOK_MAX_ATTEMPTS=8
SSE_MAX_CONNECTIONS_PER_USER=5

STORAGE_DRIVER=local
STORAGE_LOCAL_ROOT=.runtime/storage
S3_REGION=us-east-1
S3_FORCE_PATH_STYLE=false
S3_SERVER_SIDE_ENCRYPTION=AES256

SMTP_PORT=587
SMTP_SECURE=false
SMTP_FROM=BitPix <no-reply@localhost>

REQUIRE_MFA_FOR_PLATFORM=false
REQUIRE_MFA_FOR_ADMINS=false
PASSWORD_RESET_TTL_MINUTES=15
